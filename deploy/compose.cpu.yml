# CPU override for chat and embed: the non-CUDA image and no GPU reservation. Used by just model-cpu.
services:
  chat:
    image: ghcr.io/ggml-org/llama.cpp:server
    deploy: !reset null
  embed:
    image: ghcr.io/ggml-org/llama.cpp:server
    deploy: !reset null
