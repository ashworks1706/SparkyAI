# serenity::Error is 136 bytes and every Discord call returns it, so the default 128-byte
# threshold rejects every function that forwards one. Raised past it rather than allowing
# result_large_err, which would stop the lint checking the error types we do control.
large-error-threshold = 160
