# data

Not committed except this file. See `.gitignore`.

- `raw/` — teacher outputs and redacted trace exports
- `processed/` — verified SFT trajectories (`sft.jsonl`), GRPO prompts
- `preference/` — DPO pairs (`dpo.jsonl`)
