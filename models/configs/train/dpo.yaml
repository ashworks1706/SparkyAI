base_model: outputs/sft/merged
output_dir: outputs/dpo
dataset: data/preference/dpo.jsonl
max_length: 8192
beta: 0.1
lora:
  r: 16
  alpha: 32
train:
  epochs: 1
  per_device_batch_size: 1
  gradient_accumulation: 16
  learning_rate: 5.0e-6
  bf16: true
wandb:
  project: sparky-models
