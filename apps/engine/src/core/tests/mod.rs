//! The engine test suite. One file per unit under test; shared doubles in `support`.

mod agent;
mod assemble;
mod config;
mod context;
mod limit;
mod mcp;
mod policy;
mod postgres;
mod rig_mapping;
mod support;
mod trace;
