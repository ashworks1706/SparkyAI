/** Where the source lives. One place, so every link on the page agrees. */
export const REPO = "https://github.com/ashworks1706/SparkyAI";

/** The architecture write-up, linked from the how-it-works and open-source sections. */
export const ARCHITECTURE = `${REPO}/blob/main/docs/ARCHITECTURE.md`;

/** The roadmap, linked from the navigation. */
export const ROADMAP = `${REPO}/blob/main/docs/ROADMAP.md`;
