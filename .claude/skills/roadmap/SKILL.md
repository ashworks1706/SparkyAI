---
name: roadmap
description: Report progress against docs/ROADMAP.md or record a completed item. Use when asked "where are we", when finishing a roadmap bullet, or when a phase's exit condition is met.
---

- To report: read `docs/ROADMAP.md`, inspect `apps/` and tests, and list per current phase which bullets exist in code, which are partial, which are missing. Be concrete — cite files. Don't speculate.
- To record: mark the bullet done with `~~strikethrough~~` only if the code and a test exist. Don't add new bullets, sub-phases, or prose. If a phase exit condition is met, say so explicitly in the report.
- If work in the repo diverges from the roadmap, say so and propose the smallest roadmap edit, don't silently rewrite it.
